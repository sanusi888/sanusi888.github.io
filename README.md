# Sanusi Opeyemi Sanusi — Portfolio

This is a static portfolio site built with HTML/CSS/JS. Ready to deploy on GitHub Pages.

## How to deploy to GitHub Pages

1. Create a new repository on GitHub (eg. `sanusi-portfolio`).
2. Add these files and folders to the repo.
3. Example git commands:
   ```bash
   git init
   git add .
   git commit -m "Initial portfolio"
   git branch -M main
   git remote add origin git@github.com:yourusername/sanusi-portfolio.git
   git push -u origin main
